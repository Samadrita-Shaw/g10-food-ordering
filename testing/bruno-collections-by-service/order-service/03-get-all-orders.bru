meta {
  name: Get All Orders
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/orders
  body: none
  auth: none
}

tests {
  test("Orders retrieved successfully", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an('array');
  });
}