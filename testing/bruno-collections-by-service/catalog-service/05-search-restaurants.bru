meta {
  name: Search Restaurants
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/restaurants/search?query=pizza
  body: none
  auth: none
}

tests {
  test("Restaurant search successful", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an('array');
  });
}