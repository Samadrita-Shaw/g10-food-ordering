meta {
  name: Get Restaurants by Cuisine
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/restaurants/cuisine/Italian
  body: none
  auth: none
}

tests {
  test("Restaurants by cuisine retrieved successfully", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an('array');
  });
}