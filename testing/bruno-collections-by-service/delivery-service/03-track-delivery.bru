meta {
  name: Track Delivery
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/delivery/track/{{deliveryId}}
  body: none
  auth: none
}

tests {
  test("Delivery tracking retrieved successfully", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.have.property('deliveryId');
    expect(res.body).to.have.property('status');
    expect(res.body).to.have.property('currentLocation');
  });
}